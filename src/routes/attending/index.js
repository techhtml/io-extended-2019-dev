import { h } from 'preact';
import style from './style';
import Hero from '../../components/hero';

const Attending = () => (
	<div class="home io-2019-extended">
		<Hero title="Attending" />
	</div>
);

export default Attending;
